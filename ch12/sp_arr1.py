import numpy as np
b1 = np.ones(10) # 1로 10개
print(b1)
b2 = np.zeros((5,5)) # 0으로 5행 5열
print(b2)
ar1 = np.eye(2, dtype=int) # 정수로 단위행열(항등행열)
print(ar1)
ar2 = np.eye(3, dtype=int) # 정수로 단위행열(항등행열)
print(ar2)